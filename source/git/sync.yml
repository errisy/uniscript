script:
  parameters:
    inputMessage$: '@{arg0}'
  steps:
  - name: git sync
    pre: |
      $['message'] = $['inputMessage'] || 'Unnamed Commit';
    cmd: git add -A && git commit -a -m "@{message}" && git pull && git push
    ignoreError: True